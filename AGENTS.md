# 编码助手工作约定

## 开始工作前

1. 完整阅读 `PROJECT.md`。
2. 阅读 `docs/PRODUCT_PRINCIPLES.md`、`docs/DATA_MODEL.md`、`docs/STATE_RULES.md`、`docs/NAVIGATION.md`。
3. 根据本次需求阅读 `docs/DECISIONS.md`、`docs/EXTERNAL_INTEGRATIONS.md`、`docs/TEST_CHECKLIST.md` 和 `docs/KNOWN_ISSUES.md`。
4. 检查 Git 状态、当前分支、版本号和最近提交；不得覆盖用户未提交的改动。

## 修改前必须判断

新需求属于哪一类：局部视觉、单页交互、导航入口、状态规则、数据结构、安全恢复、兼容性、外部设备或跨设备协作。

只要涉及导航、状态、数据或安全，就必须检查：

- 是否影响旧数据和数据库迁移；
- 是否影响公司/任务数据隔离；
- 是否产生重复入口或功能丢失；
- 是否改变返回路径、空状态和完成反馈；
- 是否影响导入、导出、备份和恢复；
- 是否需要新增自动或人工验收项目。

## 不可破坏的产品规则

- 同一功能只有一个主要入口；辅助入口必须有清晰的情境价值。
- 返回始终回到实际上一步，只有明确的流程完成才返回根页面。
- 无数据时给出明确下一步；操作完成后给出结果和推荐下一步。
- 当前公司页面不得混入其他公司的任务或数据。
- 不同公司允许相同资产编号；同一公司的资产编号唯一。
- 扫码高频流程尽量无确认；传输二维码必须“核对并导入”。
- 删除、覆盖、恢复必须说明对象和范围，并优先创建保护性备份。
- 导入不得静默覆盖；重复导入应可识别并保持幂等。
- 完成、归档和重新开启必须遵守 `docs/STATE_RULES.md`。

## 开发和验证

- 使用纵向切片：入口、操作、保存、反馈、返回和异常处理一起完成。
- 能自动测试的事项不要留给用户。
- 外设、语音、相机、网络切换和真实打印必须标记为“待真机验收”，不能以编译通过代替。
- 常规校验命令：

```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:testDebugUnitTest :app:assembleDebug
```

- 真机连接时安装并做启动冒烟检查。

## 完成工作前

- 更新 versionCode 与 versionName；同一交付版本不得重复使用版本号。
- 更新 `PROJECT.md`、相关 `docs/` 和 `docs/TEST_CHECKLIST.md`。
- 检查功能丢失、重复入口、跨公司泄漏、状态刷新、空状态、返回和完成反馈。
- 报告哪些已自动验证、哪些仍需用户人工验证。
- 用户要求上传时，只提交本次范围内文件，并推送现有远程分支。
